# Supabase (use ANON key here, NOT service_role — RLS protects the data)
NEXT_PUBLIC_SUPABASE_URL=https://omwbjwptmuvyqsqfpbws.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# The site URL (used for auth callbacks). Set per environment.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Auth: email allowlist (comma-separated, lowercase). Anyone not on this list
# gets bounced after login. Leave empty to allow any email that completes auth.
ALLOWED_EMAILS=coaching@coreyralston.pro
